version: '3.7'

services:
  lanchonete:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app/src
    working_dir: /app/src
    environment:
      - MONGODB_URL=${MONGODB_URL}
    networks:
      - default

networks:
  default:
    name: lanchonete_produto_network
